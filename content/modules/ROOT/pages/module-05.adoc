このAsciiDocテキストは、Ansible Lightspeedを使用してMicrosoft Azureの仮想マシン（VM）をプロビジョニングするAnsible Playbookを生成する手順を説明したラボガイドです。

以下に、対応する日本語訳を提供します。

---

= ラボガイド: Ansible Lightspeedを使用したAzure VMのプロビジョニング
:doctype: book
:notoc:
:toc-title: 目次
:nosectnums:
:icons: font

_Ansible Lightspeedを使用して、Microsoft Azureに仮想マシンをプロビジョニングするプレイブックを生成するためのガイドです。_

---

== はじめに - セットアップ

[IMPORTANT]
====
**このラボに関する重要な注意点**

IBM watsonx Code Assistantモデルは継続的に更新され、改善されています。その結果、モデルの更新により、このラボで説明されている特定のタスク生成の課題が、説明どおりに機能しない場合があります。これは予期される動作です。

**このラボの主な目的は、Ansible Lightspeedの主要な機能、特に以下について理解を深めることです。**

* **プレイブック生成** - 自然言語のプロンプトからプレイブック全体を生成する
* **タスク生成** - 個別のタスク、または複数のタスクを一度に生成する
* **トレーニングの一致** - AIサジェスチョンの透明性を理解する
* **ベストプラクティス** - LightspeedがどのようにAnsibleのベストプラクティスを取り入れているかを学ぶ

**特定のプロンプトが期待どおりにサジェスチョンを生成しない場合:**

* タスク生成ではなく**プレイブック生成**を試す（Ansibleアイコンをクリック → **Generate a playbook**ボタンをクリックし、プロンプトを入力、アウトラインをレビューしてからContinueをクリックします）
* 最適なサジェスチョンを得るために、さまざまなプロンプトのバリエーションを試す
* 複雑なマルチタスクのプロンプトを、個別のシングルのタスクのプロンプトに単純化する
* 学習目標は変わりません。AIアシストによるAnsible開発の取り組み方を理解することです。
====

この課題では、**Ansible Lightspeed**を使用して、Microsoft Azure仮想マシン（VM）のプロビジョニングを自動化する**Ansible Playbook**を生成します。

=== あなたのミッション

あなたの目標は、Ansible Lightspeedを使用して、次のタスクを自動化するプレイブックを生成することです。

1.   ラボから提供されたデモAzureアカウントに、`vm-lightspeed` というVMをプロビジョニングする。

The examples used in this lab are available in the link:[https://github.com/ansible/ansible-lightspeed-demos](https://github.com/ansible/ansible-lightspeed-demos)[Ansible Lightspeed Demoリポジトリ]。

[NOTE]
====
**Azure認証情報:** このラボでは一時的なAzureアカウントを使用します。認証情報は、タスク4で必要になるときに、ラボ環境の上部にある**AWS/Azure**タブにあります。

**ヘルプが必要な場合:** 困難に遭遇した場合や、作業を確認したい場合は、リポジトリ内の `playbooks/cloud/azure/solution_provision_azure_vm.yml` にある解決策プレイブックを参照できます。
====

---

## タスク1: Ansible Lightspeedを使用したプレイブックタスクの生成

事前に作成されたプレイブックファイルを編集し、自然言語のプロンプトを使用して、Ansible Lightspeedに必要なタスクを生成させます。

1.   **VS Codeでプレイブックファイルを開きます。**
+
    a. ラボ環境の上部にある**VS Code**タブをクリックします。
+
[IMPORTANT]
====
**新しいブラウザタブで作業:** Ansible Lightspeedの機能を最大限に活用するには、インラインの埋め込みウィンドウを使用するのではなく、**VS Codeタブをクリックして新しいブラウザタブで開いてください**。
====
+
    b. VS Codeの左側にある**Explorer**アイコン（2つのドキュメントが重なっているように見えます）をクリックして、ファイルエクスプローラーを表示します。
+
    c. ファイルエクスプローラーで、**playbooks** → **cloud** → **azure**のフォルダを展開します。
+
    d. **demo_provision_azure_vm.yml**をクリックして、エディターで開きます。

2.   **VMプロビジョニングタスクを生成します。**
+
    a. プレイブックファイル内で、次の行を見つけます。
+
[source,yaml]
----
    #- name: Create a VM called vm-lightspeed using vm_config var
----
+
    b. 行の先頭にある**`#` 記号を削除します**（または Windows/Linuxでは `CTRL+/`、Macでは `CMD+/` を押します）。
+
    c. **カーソルをその行の行末**（"var"の後）に置きます。
+
    d. **`ENTER` を押して**新しい行を作成します。
+
    e. 少し待つと、Ansible Lightspeedが**グレーのテキストでサジェスチョン**を表示します。
+
    f. **`TAB` を押して**サジェスチョンを受け入れます。
+
    _Lightspeedの注目点:_ Lightspeedは `azure.azcollection.azure_rm_virtualmachine` モジュールを正しく使用して、Azure仮想マシンを作成するタスクを生成します。サジェスチョンは、プレイブックの `vars` セクションにある `vm_config` 変数も正しく使用しています。

3.   **プレイブックを保存します。**
+
    トップメニューから**File** → **Save**をクリックするか、`CTRL+S` (Windows/Linux) または `CMD+S` (Mac) を押します。

---

## タスク2: Ansible Lightspeedトレーニングの一致の確認

Ansible Lightspeedの主要な差別化要因の1つは、サジェスチョンの生成に使用された可能性のあるトレーニングデータに関する情報を提供することです。

1.   **Lightspeed Training Matchesビューを開きます。**
+
    a. VS Codeの上部にあるメニューバーで**View**をクリックします。
+
    b. **Open View...** を選択します。
+
    c. 表示される検索ボックスに `Lightspeed` と入力します。
+
    d. リストから**Lightspeed Training Matches**をクリックします。

2.   **トレーニングの一致を調べます。**
+
    a. プレイブック内で、生成したタスク名をクリックします。
+
    b. 生成されたコードの1行を削除し、`ENTER` を押してサジェスチョンを再生成します。
+
    c. **Lightspeed Training Matches**ペイン（通常はVS Codeの下部にあります）を確認します。
+
    d. 潜在的なトレーニングソースが表示されます。**任意の項目をクリック**して、コンテンツソース、作成者、ライセンス情報などの詳細を確認します。

---

## タスク3: プレイブックの実行

これで、完成したプレイブックを、Automation Controllerまたは `ansible-navigator` のいずれかを使用して実行することを選択できます。

### オプション1: Automation Controllerの使用

1.   **プレイブックをGitにコミットしてプッシュします。**
+
    a. VS Codeの左側にある**Source Control**アイコン（円が付いたブランチのように見えます）をクリックします。
+
    b. 「Changes」の下に `demo_provision_azure_vm.yml` が表示されているはずです。
+
    c. ファイル名にカーソルを合わせ、表示される**+**（プラス）アイコンをクリックして変更をステージングします。
+
    d. 上部の「Message」ボックスに、`Provision Azure VM` のようなコミットメッセージを入力します。
+
    e. **Commit**ボタン（チェックマークアイコン）をクリックします。
+
    f. **Sync Changes**ボタンをクリックして、プレイブックをGitリポジトリにプッシュします。

2.   **Automation Controllerからプレイブックを実行します。**
+
    a. ラボウィンドウの上部にある**Ansible Automation Platform**タブをクリックします。
+
    b. ログイン画面が表示された場合は、以下を入力します。
       * **ユーザー名:** `admin`
       * **パスワード:** `ansible123!`
+
    c. 左側のサイドバーで**Resources** → **Templates**をクリックします。
+
    d. **Provision Azure VM**という名前のテンプレートを見つけます。
+
    e. その行の右側にある**起動アイコン**（🚀ロケット船）をクリックします。
+
    f. ジョブが正常に完了するまで待ちます（緑色の「Successful」ステータスが表示されます）。

### オプション2: ansible-navigatorの使用

1.   **VS Codeでターミナルを開きます。**
+
    a. **VS Code**タブにいることを確認します。
+
    b. 上部のメニューバーを見て、**Terminal** → **New Terminal**をクリックします。
+
    c. ターミナルパネルがVS Codeの下部に開きます。

2.   **プレイブックフォルダに移動します。**
+
    ターミナルで、次のコマンドを入力し、`ENTER` を押します。
+
[source,bash]
----
cd playbooks/cloud/azure
----

3.   **ansible-navigatorでプレイブックを実行します。**
+
    次のコマンドを入力し、`ENTER` を押します。
+
[source,bash]
----
ansible-navigator run demo_provision_azure_vm.yml
----
+
    プレイブックが実行され、結果が表示されます。完了したら `ESC` キーを押してターミナルプロンプトに戻ります。

---

## タスク4: VMプロビジョニングの確認

最後に、新しい仮想マシンがAzure Portalで実行されていることを確認します。

1.   **Azure認証情報を取得し、ポータルにアクセスします。**
+
    a. ラボ環境の一番上にある**AWS/Azure**タブをクリックします。
+
    b. **Azure**認証情報セクションまでスクロールします。
+
    c. Azureアカウントの認証情報（サブスクリプションID、メール、パスワード、ポータルURL）が表示されます。
+
    d. **Azure Portal URL**リンクをクリックします（または新しいブラウザタブにコピーします）。

2.   **Azure Portalにログインします。**
+
    a. **AWS/Azure**タブに戻って認証情報をコピーします。
+
    b. Azureセクションに表示されている**Email**と**Password**をコピーします。
+
    c. それらをAzureログインページに貼り付けます。
+
    d. **Sign In**をクリックします。
+
    e. 追加のセキュリティの質問が表示された場合は、**Skip for now**または**Ask later**をクリックします。

3.   **Azure VMを表示します。**
+
    a. Azure Portalで、上部の検索バーを探します。
+
    b. `Virtual machines` と入力し、結果から**Virtual machines**をクリックします。
+
    c. リストに `vm-lightspeed` という名前の新しいVMが表示されているはずです。
+
    d. ステータスは「Running」と表示されているはずです。
+
    e. これにより、AnsibleプレイブックがAzure VMの作成に成功したことが確認されます！

---

== 結論と追加リソース

おめでとうございます！Ansible Lightspeedを使用して、AzureにVMをデプロイするためのAnsible Playbookの作成に成功しました！

[NOTE]
====
**解決策プレイブックが入手可能:** 問題が発生した場合や、作業内容を比較したい場合は、リポジトリ内の `playbooks/cloud/azure/solution_provision_azure_vm.yml` で完全な解決策を確認できます。
====

=== 追加リソース

* IBM watsonx Code Assistantを使用したRed Hat Ansible Lightspeedに関するlink:[https://www.redhat.com/en/technologies/management/ansible/ansible-lightspeed](https://www.redhat.com/en/technologies/management/ansible/ansible-lightspeed)[詳細情報]
* link:[https://docs.redhat.com/en/documentation/red_hat_ansible_lightspeed_with_ibm_watsonx_code_assistant](https://docs.redhat.com/en/documentation/red_hat_ansible_lightspeed_with_ibm_watsonx_code_assistant)[Ansible Lightspeedドキュメント]
* link:[https://learn.microsoft.com/en-us/azure/virtual-machines/](https://learn.microsoft.com/en-us/azure/virtual-machines/)[Azure Virtual Machinesドキュメント]
* link:[https://www.youtube.com/ansibleautomation](https://www.youtube.com/ansibleautomation)[Red Hat Ansible Automation Platform YouTubeチャンネル]

=== フィードバック

VS Code Ansible Extensionサイドバーにある**Ansible Lightspeed Feedback**セクションを使用して、フィードバックを提供するか、バグを報告してください。
