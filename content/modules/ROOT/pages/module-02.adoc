このAsciiDocテキストは、Ansible Lightspeedを使用してCockpitをデプロイおよび設定するAnsible Playbookを生成する手順を説明したラボガイドです。

以下に、対応する日本語訳を提供します。

---

= ラボガイド: Ansible Lightspeedを使用したCockpitのデプロイ
:doctype: book
:notoc:
:toc-title: 目次
:nosectnums:
:icons: font

_Ansible Lightspeedを使用して、Red Hat Enterprise LinuxにCockpitウェブコンソールをデプロイおよび設定するプレイブックを生成するためのガイドです。_

---

== はじめに - セットアップ

[IMPORTANT]
====
**このラボに関する重要な注意点**

IBM watsonx Code Assistantモデルは継続的に更新され、改善されています。その結果、モデルの更新により、このラボで説明されている特定のタスク生成の課題が、説明どおりに機能しない場合があります。これは予期される動作です。

**このラボの主な目的は、Ansible Lightspeedの主要な機能、特に以下について理解を深めることです。**

* **プレイブック生成** - 自然言語のプロンプトからプレイブック全体を生成する
* **タスク生成** - 個別のタスク、または複数のタスクを一度に生成する
* **トレーニングの一致** - AIサジェスチョンの透明性を理解する
* **ベストプラクティス** - LightspeedがどのようにAnsibleのベストプラクティスを取り入れているかを学ぶ

**特定のプロンプトが期待どおりにサジェスチョンを生成しない場合:**

* タスク生成ではなく**プレイブック生成**を試す（Ansibleアイコンをクリック → **Generate a playbook**ボタンをクリックし、プロンプトを入力、アウトラインをレビューしてからContinueをクリックします）
* 最適なサジェスチョンを得るために、さまざまなプロンプトのバリエーションを試す
* 複雑なマルチタスクのプロンプトを、個別のシングルのタスクのプロンプトに単純化する
* 学習目標は変わりません。AIアシストによるAnsible開発の取り組み方を理解することです。
====

この課題では、Ansible Lightspeedを使用して、RHELウェブコンソールであるCockpitのデプロイと設定を自動化するAnsible Playbookを生成します。

=== あなたのミッション

あなたの目標は、Ansible Lightspeedを使用して、次のタスクを自動化するプレイブックを生成することです。

1.   `redhat.rhel_system_roles` コレクションを使用して `cockpit` をインストールする。
2.   カスタムの `cockpit.conf` 設定ファイルをターゲットホストにコピーする。
3.   `cockpit` サービスを開始し、有効にする。
4.   サービスが利用可能であることを検証する。

このラボで使用される例は、link:[https://github.com/ansible/ansible-lightspeed-demos](https://github.com/ansible/ansible-lightspeed-demos)[Ansible Lightspeed Demoリポジトリ]で入手可能です。

[NOTE]
====
**ヘルプが必要な場合:** 困難に遭遇した場合や、作業を確認したい場合は、リポジトリ内の `playbooks/infra/install_cockpit/solution_install_cockpit.yml` にある解決策プレイブックを参照できます。
====

---

## タスク1: Ansible Lightspeedを使用したプレイブックタスクの生成

事前に作成されたプレイブックファイルを編集し、自然言語のプロンプトを使用して、Ansible Lightspeedに必要なタスクを生成させます。

1.   **VS Codeでプレイブックファイルを開きます。**
+
    a. ラボ環境の上部にある**VS Code**タブをクリックします。
+
[IMPORTANT]
====
**新しいブラウザタブで作業:** Ansible Lightspeedの機能を最大限に活用するには、インラインの埋め込みウィンドウを使用するのではなく、**VS Codeタブをクリックして新しいブラウザタブで開いてください**。
====
+
    b. VS Codeの左側にある**Explorer**アイコン（2つのドキュメントが重なっているように見えます）をクリックして、ファイルエクスプローラーを表示します。
+
    c. ファイルエクスプローラーで、**playbooks** → **infra** → **install_cockpit**のフォルダを展開します。
+
    d. **demo_install_cockpit.yml**をクリックして、エディターで開きます。

2.   **`cockpit` のインストールタスクを生成します。**
+
    a. プレイブックファイル内で、次の行を見つけます。
+
[source,yaml]
----
    #- name: Install cockpit using redhat.rhel_system_roles collection
----
+
    b. 行の先頭にある**`#` 記号を削除します**。
       * `#` を手動で削除するか、または、
       * その行のどこかをクリックし、`CTRL+/` (Windows/Linux) または `CMD+/` (Mac) を押してコメントアウトを解除します。
+
    c. **カーソルをその行の行末**（"collection"の後）に置きます。
+
    d. **`ENTER` を押して**新しい行を作成します。
+
    e. 少し待つと、Ansible Lightspeedが**グレーのテキストでサジェスチョン**を表示します。
+
    f. **`TAB` を押して**サジェスチョンを受け入れます。
+
    _Lightspeedの注目点:_ サジェスチョンは、プロンプトで指定されたとおりに `redhat.rhel_system_roles.cockpit` ロールを正しく使用しており、**完全修飾コレクション名 (FQCN)** の使用など、ベストプラクティスを取り入れています。

3.   **ファイルコピーのタスクを生成します。**
+
    a. 次のコメントアウトされた行を見つけます。
+
[source,yaml]
----
    #- name: Copy cockpit.conf.j2 to /etc/cockpit directory
----
+
    b. `#` を削除して**行のコメントアウトを解除します**（または `CTRL+/` / `CMD+/` を押します）。
+
    c. **カーソルをその行の行末**（"directory"の後）に置きます。
+
    d. **`ENTER` を押して**新しい行を作成します。
+
    e. Lightspeedがサジェスチョンを表示したら**`TAB` を押します**。
+
    _Lightspeedの注目点:_ プロンプトにある `.j2` ファイル拡張子に基づいて、Lightspeedは `ansible.builtin.template` モジュールを使用する必要があることを正しく推測し、正しいファイルパーミッションを設定します。

4.   **サービスを管理するためのマルチタスクを生成します。**
+
[IMPORTANT]
====
**マルチタスクのプロンプトは動作が異なります。** これらは単一の `#` で始まり、複数のタスクをアンパサンド (`&`) で区切ります。そして、**コメントアウトを解除してはいけません**。コメント行の最後に `ENTER` を押すだけです。
====
+
    a. 次の行を見つけます。
+
[source,yaml]
----
    # Restart and enable service & wait 15 seconds for it to be available
----
+
    b. **この行のコメントアウトを解除しないでください** - `#` 記号はそのままにしておきます。
+
    c. **カーソルをその行の行末**（"available"の後）に置きます。
+
    d. **`ENTER` を押して**新しい行を作成します。
+
    e. Lightspeedが**両方のタスク**のサジェスチョンを表示したら**`TAB` を押します**。
+
    _Lightspeedの注目点:_ Lightspeedはプレイブック全体のコンテキストを使用して、再起動するサービスが `cockpit` であると推測します。次に、`&` 区切り文字を使用して2番目のタスクを識別し、`wait_for` モジュールでデフォルトのCockpitポート (9090) を正しく使用します。

5.   **プレイブックを保存します。**
+
    トップメニューから**File** → **Save**をクリックするか、`CTRL+S` (Windows/Linux) または `CMD+S` (Mac) を押します。

---

## タスク2: Ansible Lightspeedトレーニングの一致の確認

Ansible Lightspeedの主要な差別化要因の1つは、サジェスチョンの生成に使用された可能性のあるトレーニングデータに関する情報を提供することです。

1.   **Lightspeed Training Matchesビューを開きます。**
+
    a. VS Codeの上部にあるメニューバーで**View**をクリックします。
+
    b. **Open View...** を選択します。
+
    c. 表示される検索ボックスに `Lightspeed` と入力します。
+
    d. リストから**Lightspeed Training Matches**をクリックします。

2.   **トレーニングの一致を調べます。**
+
    a. プレイブック内で、生成した任意のタスク名をクリックします。
+
    b. 生成されたコードの1行を削除し、`ENTER` を押してサジェスチョンを再生成します。
+
    c. **Lightspeed Training Matches**ペイン（通常はVS Codeの下部にあります）を確認します。
+
    d. 潜在的なトレーニングソースが表示されます。**任意の項目をクリック**して、コンテンツソース、作成者、ライセンス情報などの詳細を確認します。

---

## タスク3: プレイブックの実行

これで、完成した `demo_install_cockpit.yml` プレイブックを、Automation Controllerまたは `ansible-navigator` のいずれかを使用して実行することを選択できます。

### オプション1: Automation Controllerの使用

1.   **プレイブックをGitにコミットしてプッシュします。**
+
    a. VS Codeの左側にある**Source Control**アイコン（円が付いたブランチのように見えます）をクリックします。
+
    b. 「Changes」の下に `demo_install_cockpit.yml` が表示されているはずです。
+
    c. ファイル名にカーソルを合わせ、表示される**+**（プラス）アイコンをクリックして変更をステージングします。
+
    d. 上部の「Message」ボックスに、`Install Cockpit` のようなコミットメッセージを入力します。
+
    e. **Commit**ボタン（チェックマークアイコン）をクリックします。
+
    f. **Sync Changes**ボタンをクリックして、プレイブックをGitリポジトリにプッシュします。

2.   **Automation Controllerからプレイブックを実行します。**
+
    a. ラボウィンドウの上部にある**Ansible Automation Platform**タブをクリックします。
+
    b. ログイン画面が表示された場合は、以下を入力します。
       * **ユーザー名:** `admin`
       * **パスワード:** `ansible123!`
+
    c. 左側のサイドバーで**Resources** → **Templates**をクリックします。
+
    d. **Deploy monitoring**という名前のテンプレートを見つけます。
+
    e. その行の右側にある**起動アイコン**（🚀ロケット船）をクリックします。
+
    f. ジョブが正常に完了するまで待ちます（緑色の「Successful」ステータスが表示されます）。

### オプション2: ansible-navigatorの使用

1.   **VS Codeでターミナルを開きます。**
+
    a. **VS Code**タブにいることを確認します。
+
    b. 上部のメニューバーを見て、**Terminal** → **New Terminal**をクリックします。
+
    c. ターミナルパネルがVS Codeの下部に開きます。

2.   **プレイブックフォルダに移動します。**
+
    ターミナルで、次のコマンドを入力し、`ENTER` を押します。
+
[source,bash]
----
cd playbooks/infra/install_cockpit
----

3.   **ansible-navigatorでプレイブックを実行します。**
+
    次のコマンドを入力し、`ENTER` を押します。
+
[source,bash]
----
ansible-navigator run demo_install_cockpit.yml
----
+
    プレイブックが実行され、結果が表示されます。完了したら `ESC` キーを押してターミナルプロンプトに戻ります。

---

## タスク4: Cockpitインストールの確認

最後に、Cockpitウェブインターフェースにアクセスできることを確認します。

1.   **Cockpitウェブインターフェースを開きます。**
+
    ラボ環境の一番上にある**Cockpit**タブをクリックします。

2.   **Cockpitが実行されていることを確認します。**
+
    プレイブックが正常に完了した場合、Cockpitウェブコンソールインターフェースが表示されるはずです。これにより、Cockpitが正しくインストール、設定、および起動されたことが確認されます。

---

== 結論と追加リソース

おめでとうございます！Ansible Lightspeedを使用して、CockpitをデプロイするためのAnsible Playbookの作成に成功しました！

[NOTE]
====
**解決策プレイブックが入手可能:** 問題が発生した場合や、作業内容を比較したい場合は、リポジトリ内の `playbooks/infra/install_cockpit/solution_install_cockpit.yml` で完全な解決策を確認できます。
====

=== 追加リソース

* IBM watsonx Code Assistantを使用したRed Hat Ansible Lightspeedに関するlink:[https://www.redhat.com/en/technologies/management/ansible/ansible-lightspeed](https://www.redhat.com/en/technologies/management/ansible/ansible-lightspeed)[詳細情報]
* link:[https://docs.redhat.com/en/documentation/red_hat_ansible_lightspeed_with_ibm_watsonx_code_assistant](https://docs.redhat.com/en/documentation/red_hat_ansible_lightspeed_with_ibm_watsonx_code_assistant)[Ansible Lightspeedドキュメント]
* link:[https://www.youtube.com/ansibleautomation](https://www.youtube.com/ansibleautomation)[Red Hat Ansible Automation Platform YouTubeチャンネル]

=== フィードバック

VS Code Ansible Extensionサイドバーにある**Ansible Lightspeed Feedback**セクションを使用して、フィードバックを提供するか、バグを報告してください。
